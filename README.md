clean-all-project-rn is a simple to clean node_module folder and run gradlew clean in all project

Requirements
-------------

  1. `bash`, any recent version should work
  2. `gradle`

Examples
---------

      socat TCP4-LISTEN:8080 EXEC:/usr/local/bin/bashttpd
